# CI test marker

Throwaway file used once to confirm the `pull_request` trigger in
`.github/workflows/ci.yml` actually fires CI on a PR into `main`.
Safe to delete once verified — not project content.
