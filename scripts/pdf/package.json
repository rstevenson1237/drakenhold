{
  "name": "drakenhold-pdf",
  "private": true,
  "type": "module",
  "description": "Renders build/Drakenhold_Playbook.md to a paginated PDF. Not part of check.sh/build.sh's zero-dependency guarantee — this is a CI rendering step, per TRANSITION_TO_CLAUDE_CODE.md's own note that the PDF target needs mermaid-cli and a headless browser.",
  "scripts": {
    "render": "node render.mjs"
  },
  "dependencies": {
    "@mermaid-js/mermaid-cli": "^11.16.0",
    "marked": "^18.0.9",
    "puppeteer": "^25.7.0"
  }
}
